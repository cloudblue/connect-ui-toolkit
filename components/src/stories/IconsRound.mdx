import { Meta } from '@storybook/blocks';
import * as icons from '@cloudblueconnect/material-svg/round';
import Icon from '../widgets/icon/widget.vue';

<Meta title="Components/Icon/All Round Icons" />

# Iconography - Round Icons

<div className="all-icons">
{Object.keys(icons).map(icon => (<div className="icon-wrapper"><Icon iconName={icon} size="32"/> <span>{icon}</span></div>))}
</div>
<style>
  {`
  .all-icons{
    margin-top: 24px;
    width: 100%;
    display: flex;
    flex-wrap: wrap;

  .icon-wrapper{
    width: calc(33% - 10px);
    margin-bottom: 20px;
    margin-right: 10px;
    display: flex;
    align-items: center;

  .icon{
    margin-right: 4px;
  `}
</style>